import React from 'react';
import styles from "./styles.module.scss";
import {Box} from "@mui/material";

const ClientDivider = () => {
    return (
        <Box className={styles.clientDivider}>

        </Box>
    );
};

export default ClientDivider;
